#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_concurrently@7.3.0@concurrently/dist/bin/concurrently.js" "$@"
  ret=$?
else 
  node  "$basedir/../_concurrently@7.3.0@concurrently/dist/bin/concurrently.js" "$@"
  ret=$?
fi
exit $ret
